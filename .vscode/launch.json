{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/debug/cli_bg",
            "cwd": "${workspaceFolder}/debug",
            "args": [ "Wikipedia, the free encyclopedia", "-q16", "-dm" ]
        }
    ]
}